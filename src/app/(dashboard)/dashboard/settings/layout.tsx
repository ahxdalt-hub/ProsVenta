/**
 * Shared layout for Settings routes. The shell (heading + section navigation)
 * is applied per-page so the landing page can use its own wide container.
 */
export default function DashboardSettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
