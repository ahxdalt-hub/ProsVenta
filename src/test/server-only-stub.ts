// Test stub for the "server-only" package. Vitest runs in Node (no React
// Server Components boundary), so the import resolves to an empty module.
// The alias is wired in vitest.config.ts.
export {};
