/**
 * Shared marketing route destinations.
 */
export const MARKETING_ROUTES = {
  /** Existing signup route. */
  GET_STARTED: "/signup",
  /** Existing login route. */
  LOGIN: "/login",
  /** Explore destination — dedicated Phase 6 route for the system overview. */
  EXPLORE: "/explore",
} as const;
